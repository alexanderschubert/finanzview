name: Test Runner

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Runner testen
        run: |
          echo "========================================"
          echo "Finanzblick Gitea Runner funktioniert"
          echo "========================================"
          echo "Hostname:"
          hostname
          echo
          echo "Docker:"
          docker --version
          echo
          echo "Git:"
          git --version
