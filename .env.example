APP_NAME=FinanzView
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=http://localhost

APP_LOCALE=de
APP_FALLBACK_LOCALE=de
APP_FAKER_LOCALE=de_DE
APP_TIMEZONE=Europe/Berlin

LOG_CHANNEL=stack
LOG_LEVEL=error

DB_CONNECTION=pgsql
DB_HOST=postgres
DB_PORT=5432
DB_DATABASE=finanzblick
DB_USERNAME=finanzblick
DB_PASSWORD=CHANGE_ME
SESSION_DRIVER=database
SESSION_LIFETIME=120
CACHE_STORE=database
QUEUE_CONNECTION=database
MAIL_MAILER=log
# E-Mail (nötig für "Passwort vergessen"). Mit MAIL_MAILER=log werden
# Mails nur ins Log geschrieben.
MAIL_FROM_ADDRESS=finanzview@example.com
MAIL_FROM_NAME=FinanzView
# MAIL_MAILER=smtp
# MAIL_HOST=smtp.example.com
# MAIL_PORT=587
# MAIL_USERNAME=
# MAIL_PASSWORD=
# MAIL_SCHEME=

# Single Sign-On über OpenID Connect (z. B. Authentik)
# Redirect-URI im Provider: <APP_URL>/auth/oidc/callback
OIDC_ENABLED=false
OIDC_ISSUER=
OIDC_CLIENT_ID=
OIDC_CLIENT_SECRET=
OIDC_BUTTON_LABEL="Mit Authentik anmelden"
# OIDC_SCOPES="openid profile email"
# OIDC_TRUST_EMAIL=false
